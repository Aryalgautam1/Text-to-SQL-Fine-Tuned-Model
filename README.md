# Text-to-SQL-Fine-Tuned-Model
A fine-tuned 7B SQL specialist trained to outperform frontier models like GPT-4o on the BIRD benchmark. Includes the full pipeline: training, custom eval harness, and a live demo.
